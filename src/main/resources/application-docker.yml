spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://mysql:3306/smooth}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${JWT_SECRET}
  access-expiration: 900000      # 15분
  refresh-expiration: 1209600000 # 2주

logging:
  level:
    com.smooth.backend: ${LOG_LEVEL:DEBUG}

server:
  port: ${SERVER_PORT:8080}