# 이메일 설정 (Gmail 기준)
# Gmail > 계정 관리 > 보안 > 2단계 인증 > 앱 비밀번호 생성
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# 데이터베이스 설정
# 개발환경: localhost, 프로덕션환경: mysql (Docker 컨테이너명)
DATABASE_URL=jdbc:mysql://mysql:3306/smooth
DATABASE_USERNAME=root
DATABASE_PASSWORD=root

# JWT 설정
# 프로덕션에서는 복잡한 랜덤 문자열로 변경 필수!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=86400000

# 로깅 설정
# 개발: DEBUG, 프로덕션: INFO
LOG_LEVEL=DEBUG
JPA_SHOW_SQL=true

#포트 설정
SERVER_PORT=8080

#Spring profile 설정 (yml 선택)
SPRING_PROFILES_ACTIVE=docker

#docker-compose

# ECR 사용시
ECR_IMAGE_URI=123456789012.dkr.ecr.region.amazonaws.com/your-repo:tag

# Docker 설정
DOCKER_USERNAME=your-docker-username
IMAGE_NAME=user-service
IMAGE_TAG=0.0.1
BACKEND_PORT=8080

# 데이터베이스 설정 (개발용 - 간단한 비밀번호)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=smooth
MYSQL_USER=smooth
MYSQL_PASSWORD=smooth